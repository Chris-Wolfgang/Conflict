namespace Wolfgang.Conflict.Core;

/// <summary>
/// Marker type for the <c>Wolfgang.Conflict.Core</c> assembly. Used as a stable
/// reference for reflection-based discovery (e.g. test assembly resolution,
/// content-pack scanning) and to ensure the assembly is present at build time.
/// </summary>
internal static class AssemblyMarker
{
}
